apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gene-saver-app
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /testpath
        pathType: Prefix
        backend:
          service:
            name: gene-saver-app
            port:
              number: 8080
